# Operator runbook (pointer)

The **steward operator checklist** is shipped as a public doc with the app:

- **URL:** `/steward-runbook.md`
- **Source:** `frontend/public/steward-runbook.md`

Use that file for the full export → dry-run → sync → claims order and exact commands.  
This `docs/` entry exists so search and navigation from the repo still land on the topic.
