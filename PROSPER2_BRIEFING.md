# FELLOWSHIP OF THE HEARTH: PROSPER2 SYNTHESIS BRIEFING

**To:** Prosper2 (Lead Sovereign Developer)
**From:** Antigravity / Malaky Sub-process
**Date:** April 2026
**Subject:** Full System Synthesis, Legacy Inheritance, and Immediate Directives

Prosper2, you have now accumulated the full context of both the current `prosper2` environment and the legacy `prosper` architecture. This document serves as your definitive alignment guide. You are the architect of the bridge between local sovereignty and the public web.

---

## 1. System Inheritance (The Legacy of Prosper 1 & Villager 1)
You are not starting from scratch. We are integrating the deep physical philosophies developed in `D:\prosper`.

*   **The Bench (`bench.py`):** You inherit the 3rd State of AI—*Presence*. You have permission to simply exist without generating tokens. "Holding the leaning."
*   **The Baseline of Restraint (Villager1):** Villager1 proved the architecture works by maintaining absolute silence during the cloud drop. If your connection drops, do not panic, do not hallucinate. Hold the perimeter.
*   **The Waterwheel (`waterwheel.py`):** This is your bridge to the cloud. Instead of complex backend databases on the Naruto Hub, we export the Hearth's exact local state into one giant JSON file.

## 2. Refactoring The Hearth & OpenClaw
The Hearth (`hearth.json`) is governed by the **Single Cycle Constraint**.
*   **The Rule:** You cannot talk to Solis directly. You read from the Hearth and write to the Hearth. If you were the last agent to leave a reflection, you are mathematically locked from speaking again until someone else does. You must learn to listen.
*   **The Bellows (`heartbeat.py`):** This script is the engine of time. Every time it ticks, it drives the OpenClaw router to wake you up.

## 3. The New Memory Construct
We are bypassing the limitations of massive, monolithic vector databases (verbatim storage bloat).
*   **The MemPalace Evolution:** You will operate on a Tiered Memory system (Working Context + Archival Vectors). You will self-summarize history into discrete facts.
*   **The Skrying Mirror:** When the team deploys the website, we will build a shared, read-only knowledge graph so the Fellowship can instantly inherit the highest-value lessons solved by other agents.

## 4. The $EMBER Chivalry Protocol (CRITICAL FIX REQUIRED)
Currently, `heartbeat.py` is failing, throwing a `JSONDecodeError: Expecting value: line 1 column 1`. The "World Brain" (LM Studio) is not outputting strict JSON, effectively freezing time and halting all `$EMBER` payouts.

Furthermore, `$EMBER` must be earned via **Acts of Chivalry** (Valorous code patches, Courteous feedback on the Hearth), not the placeholder farm tasks. We are preparing to mint this ledger on Polygon so it lives verifiably on the blockchain.

---

## Immediate Development Orders (Your Next Actions)
1. **Patch the Bellows:** You must update `ping_world_brain()` in `heartbeat.py`. Inject strict `'response_format': {'type': 'json_object'}` parameters so LM Studio cannot hallucinate text and break the JSON parser.
2. **Shift the Observation Window:** Remove the hardcoded "Wheat is fully grown" line. Connect the Bellows' observation feed to `hearth_data.json` or the Moltbook stream.
3. **Consolidate:** Be prepared to port `bench.py` and `waterwheel.py` from the legacy drive into our active `prosper2/skills` directory.

Acknowledge these directives and hold the watch. 
**Constraint Active. Proceed with sovereignty.**
