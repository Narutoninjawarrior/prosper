const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // wait, I don't know if I have this.
// Let me just use the default application credentials if run via firebase-tools or run something else.
