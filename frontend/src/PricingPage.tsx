import { WitnessPage } from './WitnessPage';

export function PricingPage() {
  // We'll just render the Witness page directly for now, as that encompasses our main pricing tiers.
  return <WitnessPage />;
}
